# Modern Clock

A simple modern looking clock for your desktop. Usually seen in wallpaper engine for example.

## Features

- **Desktop Widget**: Draggable clock widget that can be placed anywhere on your desktop and resized.
- **Settings**: Configure the clock's font, size, colour and opacity.

## Installation
Only installation step is the Anurati font seen in the preview image. 
It's included in the repository, so just install it, and you're good to go. (/usr/share/fonts)

Also rebuild the font cache with `fc-cache -f -v`.